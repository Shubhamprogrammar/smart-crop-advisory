# Core AI service dependencies (installed in Phase 0).
# ML/CV/RAG-specific packages (torch, transformers, scikit-learn, opencv, OCR,
# vector DB clients) are added incrementally in the phases that introduce them
# (Phase 6 crop model, Phase 8 disease model, Phase 12 RAG) to avoid an
# unnecessarily heavy install before those features exist.

fastapi==0.121.2
uvicorn[standard]==0.38.0
pydantic==2.13.5
pydantic-settings==2.12.0
python-dotenv==1.1.1
python-multipart==0.0.20

# Phase 4 — Soil OCR (requires the `tesseract` system binary; the endpoint
# degrades gracefully with a clear error if it is not installed)
pytesseract==0.3.13
pillow==12.3.0

# Phase 6 — Crop recommendation model
scikit-learn==1.7.2
pandas==2.3.3
numpy==2.3.3
joblib==1.5.2

# Phase 11 — AI farmer assistant (Hugging Face hosted LLM via Inference Providers)
huggingface_hub==0.36.0

# Phase 8 — Disease detection (Hugging Face computer vision model)
# NOTE: this pulls the standard PyPI torch wheel (~500MB, includes CUDA
# libs) because PyTorch's CPU-only wheel index (download.pytorch.org/whl/cpu)
# has not published cp314 builds yet. CPU inference works identically either
# way. For a smaller production image on an older Python, install torch via:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
torch==2.13.0
transformers==4.57.1
